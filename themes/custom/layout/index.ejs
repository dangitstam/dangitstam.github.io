<section id="about">
  <div id="about-column">
  <% if (config.description) { %>
    <%- markdown(config.description) %>
  <% } %>
  <!-- Resume -->
  <p>
    Here is my <a href="/resume.pdf">CV</a>.
  </p>
  <% if (theme.social_links) { %>
    <p>
      <%= __('index.find_me_on') %>
      <% var nb_links = Object.keys(theme.social_links).length %>
      <% var i = 0 %>
      <% for(var link in theme.social_links) { %>
        <% if (link == 'mail') { %>
          <a class="icon" target="_blank" rel="noopener" href="<%- theme.social_links[link] %>">
            <i class="fas fa-envelope"></i><!--
      ---></a><!--
      ---><% } else { %>
          <a class="icon" target="_blank" rel="noopener" href="<%- url_for(theme.social_links[link]) %>">
            <i class="fab fa-<%= link %>"></i><!--
      ---></a><!--
    ---><% } %><!--
    ---><%= ( nb_links > 0 && i < nb_links-1 ?
            ( i == nb_links-2 ? ' '+__('index.enum_and')+' '
            : __('index.enum_comma')+' ' )
            : '.' ) %>
        <% i+=1 %>
      <% } %>
    </p>
  <% } %>
  </div>
  <div id="news-column">
    <!-- TODO: This is hard coded, find out how to config this. -->
    <span class="h1" id="news-header">News</span>
    <ul class="post-list">
      <li class="post-item">
        <div class="meta"><time>2019-06-18</time></div>
        <p>My <a href="thesis.pdf">master's thesis</a> titled "Modern Variational Methods for Semi-supervised Text Classifcation"
        was approved by my advisor and the <a href="https://www.cs.washington.edu/academics/bsms/">UW CSE BS/MS </a>reading committee!
        This completes my <strong>Master of Science in Computer Science</strong> degree at the <a href="https://washington.edu/">UW</a>.
        </p>
      </li>
      <li class="post-item">
        <div class="meta"><time>2019-05-14</time></div>
        <p>	Our paper, "Variational Pretraining for Semi-supervised Text Classification" will appear in <a href="http://www.acl2019.org/EN/index.xhtml">ACL 2019</a>.
            Read the <a href="https://arxiv.org/abs/1906.02242">arXiv preprint</a> and see the <a href="https://github.com/allenai/vampire">code</a>.</p> 
      </li>
      <li class="post-item">
        <div class="meta"><time>2019-11-30</time></div>
        <p>I have accepted an offer for a full-time position as a software engineer at <a href="https://xevo.com/">Xevo</a>. I will continue my work in applying NLP techniques to improve the driving experience.</p> 
      </li>
    </ul>
  </div>

</section>

<section id="writing">
  <span class="h1"><a href="<%- url_for("archives") %>"><%= __('index.articles') %></a></span>
  <% if (theme.tags_overview && site.tags.length) { %>
  <div id="topics">
    <span class="h2"><%= __('index.topics') %></span>
    <span class="widget tagcloud">
        <%- tagcloud({min_font: 17, max_font: 30}) %>
    </span>
  </div>
  <!-- <span class="h2"><%= __('index.most_recent') %></span> -->
  <% } %>
  <ul class="post-list">
    <% var field_sort = theme.posts_overview.sort_updated ? 'updated' : 'date' %>
    <% if (theme.posts_overview.show_all_posts) { %>
      <% var show_posts = page.posts.sort(field_sort, 'desc') %>
    <% } else { %>
      <% var show_posts = site.posts.sort(field_sort, 'desc').limit(theme.posts_overview.post_count || 5) %>
    <% } %>
    <% show_posts.each(function(post, i){ %>
      <li class="post-item">
        <%- partial('_partial/post/date', { post: post, class_name: 'meta' }) %>
        <span><%- partial('_partial/post/title', { post: post, index: true, class_name: '' }) %></span>
      </li>
    <% }); %>
  </ul>
  <% if (theme.posts_overview.show_all_posts) { %>
    <%- partial('_partial/pagination') %>
  <% } %>
</section>

<% if (site.data.projects) { %>
<section id="projects">
  <span class="h1"><a href="<%- url_for(theme.projects_url) %>"><%= __('index.projects') %></a></span>
  <ul class="project-list">
    <% for(var obj in site.data.projects){ %>
      <li class="project-item">
        <a href="<%= site.data.projects[obj].url %>"><%= site.data.projects[obj].name %></a>: <%- markdown(site.data.projects[obj].desc) %>
      </li>
    <% } %>
  </ul>
</section>
<% } %>
